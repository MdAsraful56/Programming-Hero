// Number for which we want to generate the multiplication table
const number = 9;

// Loop from 1 to 10
for (let i = 1; i <= 10; i++) {
  // Calculate the product
  const product = number * i;
    
    // Display the multiplication table
    console.log(`${number} x ${i} = ${product}`);
    }
function TenTimes(x) {
    const result = x * 10;
    return result;
}


function cutHalf(number) {
    const half = number / 2;
    return half;
}


TenTimes(10);

const outputs = TenTimes(111);
console.log(outputs);